from django.apps import AppConfig


class LoanserviceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'loanservice'
    verbose_name: str = "Gestión de Servicio de Prestamos"
